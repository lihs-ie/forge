# forge v0.1.0 Release Notes

## Highlights
- Builder + Override core API with string/enum/map factories (`factory/`).
- README/Overview docs refreshed with Usage, FAQ, architecture図解。
- Examples (`examples/basic`, `examples/enum`, `examples/map`) added for quick onboarding。
- CI pipeline (golangci-lint + go test + override policy) と Dependabot 設定を追加。
- Issue templates / project board / SemVer & support policy を整備し公開準備完了。

## Verification
```bash
GOCACHE=$(pwd)/.cache/go-build go test ./...
bash ./scripts/check_override_usage.sh
```

## Tagging
```bash
git tag v0.1.0
git push origin v0.1.0
```
